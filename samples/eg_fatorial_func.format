%% Segundo exemplo de código usado no primeiro relatório 

variables: 
	int x = factorial(4);

subprograms:

	function int factorial ( int n ): 
	   if( n != 0 ):
	      return 1;
	   %% return n * factorial( n - 1 );
	   aux = factorial( n - 1 );
	   return n * aux;
	end_function;
